{
  "name": "frogtion",
  "version": "1.3.1",
  "description": "",
  "main": "index.js",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-bundle": "browserify src/frogtion.js -o frogtion.js",
    "build-min": "browserify src/frogtion.js -o frogtion.min.js",
    "prod": "npm run build-bundle && npm run build-min",
    "minify": "uglifyjs frogtion.min.js -o frogtion.min.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
